% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-event-graph.R
\name{sim_graph}
\alias{sim_graph}
\title{Build a Simulation Graph for \code{sim_event_graph()}}
\usage{
sim_graph(..., effects = list())
}
\arguments{
\item{...}{Named \code{\link[=sim_covariate]{sim_covariate()}}/\code{\link[=sim_derived]{sim_derived()}}/\code{\link[=sim_process]{sim_process()}}
objects. Every name must be unique across all three. A \code{\link[=sim_derived]{sim_derived()}}
covariate's dependencies must each name an earlier
\code{\link[=sim_covariate]{sim_covariate()}}/\code{\link[=sim_derived]{sim_derived()}} in the same call.}

\item{effects}{List of \code{\link[=sim_effect]{sim_effect()}} objects between nodes named in
\code{...}.}
}
\value{
An object of class \code{sim_graph}.
}
\description{
\code{sim_graph} assembles a set of named \code{\link[=sim_covariate]{sim_covariate()}}/\code{\link[=sim_derived]{sim_derived()}}/
\code{\link[=sim_process]{sim_process()}} nodes and \code{\link[=sim_effect]{sim_effect()}} edges between them into a single
specification, which \code{\link[=sim_event_graph]{sim_event_graph()}} can then simulate from.
}
\examples{
graph <- sim_graph(
  age = sim_covariate(function(N) rnorm(N)),
  age_sq = sim_derived(function(age) age^2),
  censoring = sim_process("censoring", eta = 0.1, nu = 1.1),
  death = sim_process("terminal", eta = 0.1, nu = 1.1),
  effects = list(
    sim_effect("age", "death", coef = 0.5),
    sim_effect("age_sq", "death", coef = -0.05)
  )
)
graph
}
\seealso{
\code{\link[=sim_event_graph]{sim_event_graph()}}, \code{\link[=sim_covariate]{sim_covariate()}}, \code{\link[=sim_derived]{sim_derived()}},
\code{\link[=sim_process]{sim_process()}}, \code{\link[=sim_effect]{sim_effect()}}
}
