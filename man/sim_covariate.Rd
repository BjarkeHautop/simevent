% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sim-event-graph.R
\name{sim_covariate}
\alias{sim_covariate}
\title{Define a Baseline Covariate for \code{sim_graph()}}
\usage{
sim_covariate(generator)
}
\arguments{
\item{generator}{Function generating the covariate. Takes \code{N} and,
optionally, any subset of the names of other covariates defined earlier
in the same \code{\link[=sim_graph]{sim_graph()}} call, matched by argument name.}
}
\value{
An object of class \code{sim_covariate}, for use in \code{\link[=sim_graph]{sim_graph()}}.
}
\description{
Define a Baseline Covariate for \code{sim_graph()}
}
\examples{
# A covariate that doesn't depend on any other:
sim_covariate(function(N) rnorm(N, mean = 50, sd = 10))

# A covariate whose generator depends on another covariate defined
# earlier in the same sim_graph() call:
sim_covariate(function(N, age) rbinom(N, 1, plogis(-2 + 0.03 * age)))
}
\seealso{
\code{\link[=sim_graph]{sim_graph()}}, \code{\link[=sim_process]{sim_process()}}
}
